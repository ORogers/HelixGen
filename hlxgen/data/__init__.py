"""Runtime data files that ship with ``hlxgen``.

A package rather than a bare folder so the files are addressable through
:mod:`importlib.resources` and travel inside a wheel or a frozen app bundle.
See :mod:`hlxgen.resources` for the accessors.
"""
