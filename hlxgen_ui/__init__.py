"""Desktop UI for hlxgen: pick a slot, describe a tone, watch it generate,
upload it.

This package is independent of the ``hlxgen`` CLI's own dependencies -
importing it requires ``PySide6``, ``pyusb`` and ``msgpack`` (see
``requirements.txt``), none of which the plain CLI needs.
"""
