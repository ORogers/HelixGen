## What this changes

<!-- And why. If it fixes an issue, link it. -->

## How it was verified

<!-- The commands you ran. If it touches the device path, say whether it was
     tested on real hardware and on which device - a PR that could not be is
     still welcome, it just needs checking before merge. -->

- [ ] `python3 -m pytest -q`
- [ ] `python3 -m ruff check helixgen/ helixgen_ui/ tests/`

## Checklist

- [ ] No Line 6 data files or device backups are included in this diff
- [ ] `docs/application_flow.md` still describes what the code does
