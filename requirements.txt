# Core runtime dependencies
pyyaml>=6.0
openai>=3.15

# USB device support (hlxgen devices/pull/backup/push).
# pyusb also needs the libusb shared library: brew install libusb
pyusb>=1.2.1
msgpack>=1.0.5

# Desktop UI (hlxgen_ui)
PySide6>=6.6

# Development / testing utilities
pytest>=7.4
pytest-qt>=4.4
