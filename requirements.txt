# Dependencies live in pyproject.toml. This file is kept as the familiar entry
# point, and installs the project itself with every extra a contributor needs:
# the desktop UI, the USB stack, and the test and lint tooling.
#
#     python3 -m pip install -r requirements.txt
#
# For just the command-line tool, `pip install .` is enough.
#
# pyusb also needs the libusb shared library: brew install libusb
-e .[dev]
